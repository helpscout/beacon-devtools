import styled from "@helpscout/fancy";

const SelectUI = styled("select")`
  background: black;
  color: #ccc;
  font-family: inherit;
  font-size: inherit;
  display: block;
  margin: 3px 0;
`;

export default SelectUI;
